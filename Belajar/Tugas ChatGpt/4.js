/** @format */

function tambah() {
    const listHeader = document.getElementById("ul");
    const li = document.createElement("li");
    li.textContent = `list ke ${ul.children.length + 1}`;
    listHeader.appendChild(li);
}
